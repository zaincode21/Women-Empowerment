VITE_USE_MOCK=false
VITE_API_BASE=http://localhost:4000

# When developing, set VITE_USE_MOCK=true to use local in-memory mock data.
# Otherwise point VITE_API_BASE to the backend server (for example, http://localhost:4000).
